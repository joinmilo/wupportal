query getFooter {
  getMenuItems(
    params: {
      sort: "order"
    }
  ) {
    result {
      id
      header
      order
      translatables {
        id
        name
        language {
          id
          name
        }
      }
      feature {
        id
      }
      page {
        id
      }
      subMenuItems {
        id
      }
    }
  }
}
