mutation addUserRole(
  $roleId: String,
  $userId: String,
  $privilegeApplicationId: String
) {
  addUserRole(
    roleId: $roleId,
    userId: $userId,
    privilegeApplicationId: $privilegeApplicationId)
}
