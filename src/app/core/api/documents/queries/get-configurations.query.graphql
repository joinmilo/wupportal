query getConfigurations($params: FilterSortPaginateInput) {
  getConfigurations(params: $params) {
    result {
      id
      code
      value
      media {
        ...Media
      }
    }
    total
  }
}
