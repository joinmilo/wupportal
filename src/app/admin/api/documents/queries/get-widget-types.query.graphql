query getWidgetTypes($params: FilterSortPaginateInput) {
  getPageWidgetTypes(params: $params) {
    result {
      id
      code
      translatables {
        id
        description
        name
        language {
          ...Language
        }
      }
    }
    total
  }
}