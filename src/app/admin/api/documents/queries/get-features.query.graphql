query getFeatures($params: FilterSortPaginateInput) {
  getFeatures(params: $params) {
    result {
      id
      active
      code
      icon
      name
      released
      translatables{
        id
        name
        language{
          ...Language
        }
      }
    }
    total
  }
}