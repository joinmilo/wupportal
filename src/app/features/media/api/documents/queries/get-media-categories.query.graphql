query getMediaCategories($params: FilterSortPaginateInput) {
  getInfoMediaCategories(params: $params) {
    result {
      id

      translatables {
        id
        name
        language {
          id
          locale
          name
        }
      }
    }
    total
  }
}
