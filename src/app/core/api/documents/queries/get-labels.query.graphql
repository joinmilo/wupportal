query getLabels($params: FilterSortPaginateInput) {
  getLabels(params: $params) {
    result {
      id
      tagId

      translatables {
        id
        content
        language {
          ...Language
        }
      }

    }
  }
}
