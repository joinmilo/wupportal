query getReportTypes($params: FilterSortPaginateInput) {
  getReportTypes(params: $params) {
    result {
      id
      translatables {
        id
        name
        language{
          ...Language
        }
      }
    }
  }
}