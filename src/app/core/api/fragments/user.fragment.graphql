fragment User on UserEntity {
  id
  firstName
  lastName
  email
  verified
  lastLoggedIn
  userContext{
    uploads{
      profilePicture
      media {
      ...Media
      }
    }

    address {
    street
    houseNumber
    postalCode
    place
    }
  }
}