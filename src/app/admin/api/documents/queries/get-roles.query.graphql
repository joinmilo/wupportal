query getRoles($params: FilterSortPaginateInput) {
  getRoles(params: $params) {
    result {
      id
      name
      privileges {
        id
        code
      }
      users {
        id
      }
    }
    total
  }
}