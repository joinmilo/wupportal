fragment Contest on ContestEntity {
  id
  dueDate
  cardImage {
    ...Media
  }
  slug
  translatables {
    id
    name
    content
    shortDescription
    language {
      id
      locale
      name
    }
  }
}