query getFavoritingUsers($params: FilterSortPaginateInput) {
  getUserContexts(params: $params) {
    result {
      id
      user{
        id
        firstName
        lastName
        email
        }
      }
    total
  }
}