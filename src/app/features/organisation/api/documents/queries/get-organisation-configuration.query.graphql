query getOrganisationConfiguration {
  getOrganisationConfiguration(entity: {}) {
    id
    approvalRequired
    memberRole {
      id
      translatables {
        id
        name

        language {
          ...Language
        }
      }
    }
  }
}