query getFeatures($params: FilterSortPaginateInput) {
  getFeatures(params: $params) {
    result {
      id
      active
      icon
      name
      code
      translatables{
        id
        name
        language{
          ...Language
        }
      }
    }
    total
  }
}