query getPages($params: FilterSortPaginateInput) {
  getPages(params: $params) {
    result {
      id
      slug
      callText
      callUrl
      isLanding
      translatables {
        id
        name
        language {
          ...Language
        }
      }
    }
    total
  }
}