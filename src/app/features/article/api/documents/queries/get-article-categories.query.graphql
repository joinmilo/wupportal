query getArticleCategories($params: FilterSortPaginateInput) {
  getArticleCategories(params: $params) {
    result {
      id
      color
      icon
      name

      translatables {
        id
        name
        language {
          id
          locale
          name
        }
      }
    }
    total
  }
}
